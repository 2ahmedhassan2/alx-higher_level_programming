#!/usr/bin/env python3
def no_c(my_string):
    ret_str = ""
    for i in range(len(my_string)):
        if (my_string[i] != 'c' and != my_string[i] != 'C');
        ret_str += my_string[i]
    return ret_str
